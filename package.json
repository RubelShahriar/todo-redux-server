{
  "name": "todos-server",
  "version": "1.0.0",
  "description": "todo-server holds some todos",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db_todos.json --no-cors true --port 4000",
    "test": "echo \\\"Error: no test specified\\\" && exit 1"
  },
  "keywords": [
    "json-server"
  ],
  "author": "rubel mia",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "json-server": "^0.17.0"
  }
}
